<!DOCTYPE html>
<html lang="en">
  <%- include('../partials/head')%>

  <body>
    <div class="wrapper">
      <%- include('../partials/menu')%>

      <div class="main">
        <%- include('../partials/header')%>
        <main class="content refreshHero">
          <div class="container-fluid p-0">
            <div class="row">
              <div class="col-lg-12">
                <div class="card">
                  <div class="card-header bg-light">
                    Store List
                    <span class="badge bg-primary text-white"
                      ><%=storeList.length%></span
                    >
                    <div class="float-right">
                      <a href="/Stores/New" class="btn btn-primary"
                        >Add New Store</a
                      >
                    </div>
                  </div>
                  <div class="card-body">
                    <table
                      id="catTable"
                      class="table table-striped table-bordered"
                    >
                      <thead>
                        <tr>
                          <th>Store Id</th>
                          <th>Store Name</th>
                          <th>Store Phone</th>
                          <th>Store Address</th>
                          <th class="text-right">Action</th>
                        </tr>
                      </thead>
                      <tbody>
                        <%storeList.map(stores=>{%>
                        <tr>
                          <td><%=stores.storeCode%></td>
                          <td><%=stores.storeName%></td>
                          <td><%=stores.storePhone%></td>
                          <td><%=stores.storeAddress%></td>
                          <td class="text-right">
                            [<a href="">edit</a>] [<a
                              href=""
                              class="text-danger"
                              >delete</a
                            >]
                          </td>
                        </tr>
                        <%})%>
                      </tbody>
                    </table>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </main>

        <%- include('../partials/footer')%>
      </div>
    </div>
    <%- include('../partials/footerLinks')%>
    <script>
      $(document).ready(function () {
        $("#catTable").DataTable();
      });
    </script>
  </body>
</html>
